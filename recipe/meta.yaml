{% set name = "pythonnet" %}
{% set version = "3.0.0rc2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 69e40e77a9f44780c20a600361423eecf23459dd41c32d0bf34035a90643e260

build:
  number: 1
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - dotnet-sdk
  host:
    - pip
    - python >=3.7
    - setuptools >=61
    - wheel
  run:
    - clr_loader >=0.1.7
    - python >=3.7

test:
  imports:
    - clr
  requires:
    - pip
  commands:
    - pip check
    - python -c "import clr; import System.IO; from System.IO import *; assert 'FileStream' in globals()"

about:
  home: http://pythonnet.github.io
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: .Net and Mono integration for Python
  description: |
    Python for .NET is a package that gives Python programmers nearly seamless integration with the
     .NET Common Language Runtime (CLR) and provides a powerful application scripting tool for .NET
      developers.
  dev_url: https://github.com/pythonnet/pythonnet

extra:
  recipe-maintainers:
    - m-rossi
    - bgruening
